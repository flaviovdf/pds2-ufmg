#include <cassert>
#include <string>
#include <vector>

using namespace std;

void faz_algo_com_jogadores(vector<string> v) {
  assert(v.size() == 4);
  for (int i = 0; i < 4; i ++) {
    
  }
}

int main() {
  vector<string> v = {"Flavio"};
  faz_algo_com_jogadores(v);





}
